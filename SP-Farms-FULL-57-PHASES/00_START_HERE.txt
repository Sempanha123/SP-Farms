SP-FARMS — START HERE

This ZIP contains one complete prompt file for every phase.

Repository:
https://github.com/Sempanha123/SP-Farms

HOW TO USE:
1. Give this entire folder/ZIP to your AI coding agent.
2. Tell it to open `00_START_HERE.txt`.
3. It must then execute `phase01.txt` through `phase57.txt` in order.
4. It must NOT ask you to continue between phases.
5. It must use `.venv`.
6. It must test, commit, and push after every passing phase.
7. It stops only after Phase 57 production review.

MASTER INSTRUCTION TO COPY:

Read `00_START_HERE.txt`, then `00_MASTER_RULES.txt`, then execute every `phaseXX.txt`
file in numeric order.

Work autonomously.
Do not ask me to continue.
Use repository-local `.venv`.
After every phase: run tests, lint, type checks, fix failures, update progress docs,
commit, push `ai/full-production-build`, then continue immediately.
Only merge/push main and tag v1.0.0 after all production gates pass.
Missing external credentials are not blockers; use mocks/fakes and document pending live validation.


QA NOTE: Phase 17 adds LSPosed hot-reload device-profile testing only for apps/packages you own or are authorized to test; it must not target Facebook or other third-party security-sensitive apps.

This version adds Phase 21: multi-account device-pool restore queue + small per-account SP-Farms workspace backups.

This version adds Phase 52: full checkbox-driven Automation Builder with detailed per-function setup, capability-aware publishing, comments/inbox workflows, analytics, device queue integration, and presets.

This version adds Phase 53: context-aware modal tabs, no repeated target selection, per-account legitimate device bindings, Page->Account->Device inference, and strict non-blocking UI/job execution.

This version adds Phase 54: persistent per-account VPN/Proxy profiles, country/region labels, pre-restore connectivity checks, queue cleanup, and network automation integrated with Context Modals and Automation Builder.

This version adds Phase 55: Account Maintenance + Security Automation for health checks, primary info, email, password, 2FA, profile info, sessions, approvals, verification, and secure vault handling.

IMPORTANT UI REFERENCE:
Before any UI implementation, inspect:
`design/reference/sp-farms-ui-reference.png`

Follow that image as the main theme/layout reference throughout the build.
Phase 56 is the dedicated visual-fidelity and theme-lock phase.
